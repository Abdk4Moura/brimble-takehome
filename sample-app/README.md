# sample-app

A trivial Node HTTP server used as a default deployment target. Railpack auto-detects the Node provider from `package.json`'s `start` script, no Dockerfile needed.

```
PORT=3000 npm start
```
